import UI = require('ui');

import {SettingsMenu} from './SettingsMenu';

export class BenisCard extends UI.Card {

    constructor() {
        super({
            title: 'Pr0 Benis',
            subtitle: '...',
        });
    }

}
